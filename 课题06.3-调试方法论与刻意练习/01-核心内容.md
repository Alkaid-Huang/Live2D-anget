# 课题06.3：调试方法论与刻意练习（补修）

> **知识时效**
> - 文档生成日期：2026-08-13
> - 最后验证日期：2026-08-13
> - 知识领域级别：**稳定**（调试方法论、pdb/logging 基础属于多年不变的知识）
> - 主要检索源：本仓库课题01-06.2 的实战记录、Python 官方文档（pdb、logging）
> - 过期预警：稳定知识，无需强制重验

---

## 0. 跨课题知识地图

```
课题06.2：工厂 + 配置 + ServiceContext（已完成，测试全绿 9/9）
   │  你以为"测试全绿 = 代码正确"？
   │  本课直接给你答案：不是。
   ▼
课题06.3：调试方法论与刻意练习（本课）⭐
   │  在 06.2 的全绿代码里埋入 3 个 bug（测试全都抓不到）
   │  训练：复现 → 假设 → 证据 → 根因 → 修复 + 防回归
   ▼
课题06.4：mini-ASR 框架（多后端 + 流水线整合）
   │  带着调试技能进入更复杂的真实组件
   ▼
课题06.5：性能基准与剖析入门（补修）
```

**【本课三大目标】**
1. **调试五步法**：复现 → 提出假设 → 收集证据 → 定位根因 → 修复 + 防回归测试
2. **三类 bug 模式**：静默失效、契约不匹配、边界逻辑——认识它们长什么样
3. **测试盲区意识**：理解"测试全绿 ≠ 代码正确"，学会自己补上盲区

> 💡 **核心结论（后续课题会反复引用）**：调试的本质不是"修好一个 bug"，而是建立一条**现象 → 根因**的证据链。你 06.2 里踩过的 9 个错误（见 `02-错误复盘.md`）就是最好的教材——本课的 3 个 bug 和它们同类，但换了马甲。

---

## 1. 学习目标

完成本课题后，你将能够：

- 说出调试五步法，并解释每一步的目的
- 区分一个问题的"现象"和"根因"，并用证据链把两者连起来
- 用 `breakpoint()`/pdb 单步跟踪代码，观察变量变化
- 独立定位并修复 3 个预埋 bug（测试全绿状态下的真 bug）
- 为每个 bug 补一条防回归测试，验证"把 bug 改回来，测试会红"
- 用一句话向非专业人士解释"测试全绿为什么还不够"

---

## 2. 核心问题

1. 06.2 的代码测试全绿，为什么还是错的？测试到底在保护什么？
2. "现象"和"根因"有什么区别？为什么"把现象修掉"不算修完？
3. `print` 调试和调试器（断点）调试，本质区别是什么？
4. 为什么修复 bug 之后必须补防回归测试？不补会怎样？

---

## 3. 核心模型

### 3.1 调试五步法（本课骨架）

```
复现 → 提出假设 → 收集证据 → 定位根因 → 修复 + 防回归
 │         │            │            │            │
 │   列出所有可能    用日志/断点    排除法锁死   改对 + 写测试拦住
 │   的原因         /二分/对照      唯一根因
 │                                        │
 └──────── 证据不足 → 回到"提出假设"重新来 ──┘
```

**【类比/解释】** 调试不是"猜"，是**破案**：现象是案发现场，假设是嫌疑人名单，证据是监控录像。破案靠的是证据排除嫌疑人，不是靠直觉指认。

### 3.2 现象 ≠ 根因

| 层次 | 是什么 | 例子（06.2 真实发生） |
|------|--------|----------------------|
| 现象 | 你看到的不对劲 | 测试报 `TypeError: takes 1 positional argument` |
| 假设 | 可能的原因（可以有好几个） | ① 工厂签名错了 ② 参数传错了 ③ 调用方式不对 |
| 证据 | 能排除/确认假设的事实 | 看报错栈 → 问题在 `create_vad(vad_type, params)` 这一行 |
| 根因 | 唯一确定的原因 | `params` 字典没解包，被当成位置参数 |

### 3.3 三类 bug 模式（本课实战对象）

| 模式 | 特征 | 典型例子 | 为什么测试抓不到 |
|------|------|---------|-----------------|
| **静默失效** | 不报错，但行为悄悄变错 | 分贝公式系数 20 写成 10 | 测试只断言"范围"，没断言"精确值" |
| **契约不匹配** | 两处代码对"约定"理解不一致 | 配置字符串写 `silero`，判断用 `silero_vad` | 测试只检查字段存在，没检查参数透传 |
| **边界逻辑** | 判断条件差一点（off-by-one） | `>= required_hits` 写成 `>= required_hits + 1` | 测试没覆盖这条代码路径 |

---

## 4. 关键概念与例子

### 4.1 复现：稳定触发 bug 的最小条件

复现不是"多跑几次碰运气"，而是找到**最小、稳定**的触发条件。

```python
# 本课的复现脚本已经给你写好了
python reproduce.py
```

运行后你会看到三个"不对劲"的输出。你的第一步就是观察它们、描述它们（写下来），这就是**复现 + 记录现象**。

### 4.2 工具：调试器 vs print

```python
# print 调试（你会很想用，但先看看缺点）
print(db)          # 只能看这一个值，要自己决定在哪打

# 调试器（本课要求至少用一次）
breakpoint()       # 程序停在这里，进入 pdb 交互
# 常用命令:
#   n  下一步（不进入函数）
#   s  进入函数
#   p db           查看变量
#   c  继续运行到下一个断点
#   q  退出
```

**【事实】** Python 内置的 `breakpoint()` 在 Python 3.7+ 可用，等价于 `import pdb; pdb.set_trace()`。[来源：Python 官方文档 built-in functions（2026-08-13 参考）]

调试器相对 print 的优势：不用改代码就能看任何变量、可以单步走、可以停在条件满足的那一刻。**print 是"猜哪里有问题"，断点是"看哪里有问题"。**

### 4.3 证据链：二分定位

当 bug 涉及多步流程时，不要从头 print 到尾，用**二分**：先看中间结果对不对，判断问题在前半段还是后半段，再对半缩小范围。

本仓库还有一个特殊优势：`课题06.2` 是全绿的正确版本，`课题06.3` 是埋了 bug 的副本。定位到怀疑区域后，可以对照两个版本的代码：

```powershell
git diff --no-index 课题06.2-工厂模式与配置驱动\src 课题06.3-调试方法论与刻意练习\src
```

> ⚠️ 对照是**验证手段**，不是第一步。正确顺序：先用复现脚本 + 工具定位出自己的结论，再对照确认。直接 diff 看答案 = 跳过练习。

### 4.4 防回归测试

```python
def test_calculate_db_uses_20_factor():
    sm = StateMachine()
    loud = np.ones(512, dtype=np.float32) * 0.5
    db = sm.calculate_db(loud)
    expected = 20 * np.log10(0.5 + 1e-7)   # 用公式算期望，不硬编码
    assert db == pytest.approx(expected, abs=0.01)
```

防回归测试的验收标准：**把 bug 改回来，测试必须红**。如果改回 bug 测试还是绿，说明你的测试没拦住它，等于没写。

---

## 5. 常见坑

### 坑 1：把"现象"当"根因"

```python
# ❌ 看到分贝值不对，直接调 db_threshold 阈值"补偿"
#    这是修现象，根因是公式错了

# ✅ 先问：分贝值本身算对了吗？用正确公式对照
```

### 坑 2：瞎试（没有假设驱动）

```python
# ❌ 改一个值跑一次，不行再改下一个——你只是在碰运气
# ✅ 先列出 2-3 个假设，然后设计一个实验排除其中一个
```

### 坑 3：修完不补测试

```python
# ❌ 改完代码，测试全绿，收工
# ✅ 补防回归测试，并把 bug 版改回来验证测试确实会红
```

### 坑 4：print 淹没代码

调试完不删 print，或者用 print 代替断点。print 会污染代码、污染输出，而且只能看到你预先想到要看的变量。

### 坑 5：看答案

本课的 bug 和 `课题06.2` 的代码只差几行。`git diff --no-index` 一敲就能看到全部答案——但那样你就跳过了一整课的训练。**先自己查，再对照。**

---

## 6. 你的任务

> 在 `课题06.3` 的代码里，有 3 个预埋 bug。测试套件（`tests/test_06_2.py`）跑起来**全绿**，但 3 个 bug 真实存在。你的任务是找到它们、修好它们、写测试拦住它们。

### 任务 1：运行复现脚本，记录现象

```powershell
python reproduce.py
```

把三个输出如实记录到「你的作答区」：现象是什么、期望是什么、差在哪。

### 任务 2：逐个定位并修复（支架提示）

**Bug A —— 分贝值异常（静默失效）**

- 现象：同一段音频，算出来的分贝比正确公式小一半
- 排查方向：在 `calculate_db` 里断点，看返回值；对照幅度分贝公式 `20 * log10(RMS)`
- 思考：为什么原有测试没抓住它？（提示：测试只断言"静音 < -50、大声 > -20"这种范围，没断言精确值）

**Bug B —— 配置参数不生效（契约不匹配）**

- 现象：`conf.yaml`（或配置对象）里把 `prob_threshold` 改成 0.9，工厂拿到的还是 0.5
- 排查方向：在 `get_vad_params` 里断点，看 `self.vad_type` 的值，再对比 `if` 条件里写的字符串
- 思考：契约双方是谁？谁负责"约定"配置字符串的格式？

**Bug C —— 停顿后恢复多要一帧（边界逻辑）**

- 现象：`required_hits=2`，但 INACTIVE 态重新开口要第 3 帧才恢复 ACTIVE
- 排查方向：单步走进 INACTIVE 分支，观察 `hit_count` 和 `required_hits` 的比较
- 思考：这会导致什么真实后果？（提示：说话中间想词停顿，后半句开头被吞）

### 任务 3：补防回归测试

在 `tests/test_06_3.py` 里填完 3 个测试。每个测试都要满足：**bug 版代码跑它会红，修复后跑它会绿**。

### 任务 4：完整跑一遍

```powershell
python -m pytest tests\ -v
```

原有 9 个 + 你的 3 个 = 12 个，全部通过。

---

## 7. 验收标准

| 检查项 | 通过条件 |
|--------|---------|
| 复现 | `reproduce.py` 三个现象全部消失（输出显示正常/一致/生效） |
| Bug A 修复 | 分贝值符合 `20 * log10(RMS)` |
| Bug B 修复 | `conf.yaml`/配置对象的参数原样透传给工厂 |
| Bug C 修复 | INACTIVE 态在恰好 `required_hits` 帧命中时立刻回 ACTIVE |
| 防回归测试 | 3 个测试填完；把任一 bug 改回 → 对应测试失败 |
| 全套测试 | `pytest tests\ -v` 12 个全部通过 |
| 排查笔记 | 作答区记录了每个 bug 的现象/假设/证据/根因/修复/防回归 |
| 费曼核验 | 一句话解释"测试全绿为什么还不够" |

### 工程化审阅清单（本课附加）

| 审阅项 | 通过条件 |
|--------|---------|
| 证据驱动 | 排查笔记里"证据"不是"我觉得"，而是断点值/日志输出/对照结果 |
| 防回归有效 | 每个测试都能拦住对应 bug（已实测） |
| 不污染代码 | 修复后代码里没有残留调试用 print |

---

## 8. 你的作答区

### 任务 1 记录：复现脚本输出（粘贴 + 用你的话说说哪里不对劲）

```
Bug A 现象：
Bug B 现象：
Bug C 现象：
```

### 排查笔记（每个 bug 一行证据链）

```
Bug A:
  现象:
  假设:
  证据:
  根因:
  修复:
  防回归测试:

Bug B:
  ...

Bug C:
  ...
```

### 测试结果

```
python -m pytest tests\ -v 的输出粘贴在这里
```

---

## 9. 费曼技巧区

> **用一句话向非专业人士解释本课核心概念：**
>
> （提示：想想"体检报告说没病，但人就是不舒服"；或者"考试全对，但实际作业做错了"）

---

## 10. 你的反馈区

| 项目 | 你的记录 |
|------|---------|
| 完成耗时 | |
| 最大卡点 | |
| 难度自评 | |
| 对内容的理解 | （调试五步法是什么？三类 bug 各有什么特征？） |
| 改进建议 | |

---

## 附录：参考答案对照

修复完成后，用下面的命令确认自己的修复与全绿版一致（只差你修复的几行）：

```powershell
git diff --no-index 课题06.2-工厂模式与配置驱动\src 课题06.3-调试方法论与刻意练习\src
```

对照完回答两个问题：

1. 我的修复和正确答案差在哪？（实现思路、断言方式、边界考虑）
2. 如果把 06.2 的代码"重新埋一遍 bug"，你还能在 10 分钟内找到它们吗？——这是你下一次调试的起点。
